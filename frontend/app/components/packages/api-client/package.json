{
  "name": "@bistudio-feedback/api-client",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm clean && tsup src/index.ts --format esm,cjs --dts",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "tsconfig": "workspace:*",
    "tsup": "^7.2.0",
    "@types/node": "^20.5.9",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "@bistudio-feedback/core-constants": "workspace:*",
    "@ts-rest/core": "^3.28.0",
    "tsconfig": "workspace:*",
    "zod": "^3.22.4"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
